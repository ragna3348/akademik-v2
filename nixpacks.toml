[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install --omit=dev']

[phases.build]
cmds = []

[start]
cmd = 'npx prisma generate && npx prisma migrate deploy && node index.js'
